# Agent Regression Lab - in progress

Deterministic testing and regression infrastructure for LLM agent systems.

LLM agents have logs and traces, but not CI/CD - quality regression testing. Agent Regression Lab fills that gap - record agent runs, replay them deterministically, compare behavior across prompts/models/tool outputs and detect behavioral drift before it ships.

Think **git diff for agents**, not another observability dashboard.
